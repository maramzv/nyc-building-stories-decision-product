.env
__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/
venv/
data/*.csv
data/*.json
data/*.geojson
data/*.bak
!data/.gitkeep
# map.html fetches these three directly - Vercel deploys straight from git,
# so they have to actually be tracked, not just allowed through .vercelignore.
!data/map_dataset.json
!data/neighborhoods.json
!data/nyc_boundary.geojson
data/sample2983_backup/
docs/

# Claude Code chat transcripts (local records only)
.claude/save-chat-state.json

.vercel
.env*

# Local-only copies of design mockups published as Claude artifacts during
# this project's work - not part of the shipped site, kept for reference.
local-artifacts/
